/**
 * Purpose: Barrel export for RepositoryTab component
 * Scope: Clean exports for repository tab component and types
 * Overview: Standard barrel export pattern for component directory
 * Dependencies: RepositoryTab component and types
 * Exports: RepositoryTab component (default), RepositoryTabProps interface
 */

export { default, RepositoryTab } from './RepositoryTab';
export type { RepositoryTabProps } from '../../types/repository.types';
